function Num() {
  let value = "";

  return (
    <div className="">
      <p>{value}</p>

      <input placeholder="enetr one task" onChange={() => {}} />

      <button>Get Value</button>
    </div>
  );
}

export default Num;
